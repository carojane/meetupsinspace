<h1>All Meetups</h1>
<ul>
  <% @meetups.each do |meetup| %>
    <li>
      <a href="/meetups/<%= meetup.id %>">
        <%= "#{meetup.name} - #{meetup.location}" %></a>
    </li>
  <% end %>
</ul>

<p><a href="/meetups/new">Add a meetup</a></p>
